/*import React from 'react'

export default function Select({ name, values, handleChange }) {
  return (
    <select name={name} id="" onChange={handleChange}>
        {
            values.map((value) => {
                return <option value={value}>{value}</option>
            })
        }
    </select>
  )
}
*/